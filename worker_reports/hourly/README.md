# Hourly worker reports

Each role owns exactly one subdirectory. A worker writes timestamped Markdown
reports and updates only its own `STATUS.json`. Reports must contain observed
evidence, exact commands and results, changed paths, blockers, and the proposed
next goal. Claims without deterministic verification remain provisional.
