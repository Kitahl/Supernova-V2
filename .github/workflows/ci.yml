name: ci

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Unit tests
        run: python -m unittest discover -s tests -p "test_*.py"
      - name: G1-114 non-credit integration pilot
        run: python -m unittest discover -s integration -p "test_non_credit_pilot.py"
      - name: Dry-run evaluator
        run: python scripts/evaluate_goal1.py goal1/GOAL1.json examples/dry_run_records.json
